[package]
name = "lau-intention"
version = "0.1.0"
edition = "2021"
description = "The Intention Runtime — PLATO's autograd for agent intentions"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"

[lib]
name = "lau_intention"
path = "src/lib.rs"
